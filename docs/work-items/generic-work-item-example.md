# Example: Orisun Crafted Code Without Direct Authorship

## Status

<!--
Use a short prose status for the current state of the work item.
Supported values are `backlog`, `planned`, `doing`, `done`, `cancelled`, and `abandoned`.
`killed` is reserved for GitLab graveyard history when a managed work item is removed from the repository.
-->

Backlog

## Outcome

<!--
Describe the intended result of this work item in concise technical prose.
Keep this section focused on the intended outcome rather than the task list.
-->

The repository should gain a methodology note that explains how agent-assisted
software work can still count as deliberate craft when the operator sets the
standards, constraints, review loop, and acceptance bar.

## Decision Changes

<!--
Record durable design or implementation-direction decisions that changed while
shaping this work item. Do not use this section for delivery summaries.
-->

- Craft should be defined by intentional shaping and responsibility, not
  keystroke ownership.
- The note should stay grounded in repository practice instead of drifting into
  a general philosophy essay.
- The resulting outline should be reusable in Orisun-flavored documentation.

## Main Quests

<!--
List the main implementation or planning tasks required to complete this work
item.
-->

- Define what deliberate craft means in an agent-assisted workflow.
- Describe the operator responsibilities that replace line-by-line authorship.
- Identify examples or case-study angles that could ground the note.

## Acceptance Criteria

<!--
List the observable conditions that must be true for this work item to count
as complete.
-->

- The note can be used directly as a methodology outline.
- The operator responsibilities are concrete enough to be actionable.
- The note distinguishes deliberate craft from casual prompting.

## Metadata

<!--
Use this optional section to preserve stable planning attributes or override
defaults. Represent each metadata key as a `###` heading under this section.
-->

### id

<!--
Set the stable identifier for this tracked work item. It must remain stable
across renames and moves.
-->

orisun-crafted-code-01

### type

<!--
Use this only when the default `Issue` type is not enough.
Supported values are `Issue`, `OKR`, and `Test case`.
-->

Issue
